let axios = require('axios');

module.exports = {

	shareApi: function(req, res) {

		// To save what posts the employees shared.

		//   db.createPostToAll(req.body.id, req.body.content, req.body.shared], function(err, r) {
		//     res.status(200).send(req.body);
		// })

		// Set up logic to send the req to the proper provider.
		// with the id and message.

 }
}
