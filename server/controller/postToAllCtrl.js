let axios = require('axios');

module.exports = {

	postToAllApi: function(req, res) {
    
		// if database existed you could save all of your general post

		//   db.createPostToAll([req.body.post, req.body.date], function(err, r) {
		//     res.status(200).send(req.body);
		// })

		// Set up logic to send the req to the proper provider.
		// with the id and message.

 }
}
