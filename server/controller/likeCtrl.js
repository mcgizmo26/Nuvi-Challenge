let axios = require('axios');

module.exports = {

	likeApi: function(req, res) {

		// To save what posts the employees like.

		//   db.createlikePost([req.body.id, req.body.content, req.body.liked], function(err, r) {
		//     res.status(200).send(req.body);
		// })

		// Set up logic to send the req to the proper provider.
		// to like the post.

 }
}
