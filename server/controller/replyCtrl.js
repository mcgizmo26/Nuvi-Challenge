let axios = require('axios');

module.exports = {

	replyApi: function(req, res) {
		// if database existed you could save all of your reply to users

		//   db.createReply([req.body.reply, req.body.id, req.body.provider], function(err, r) {
		//     res.status(200).send(req.body);
		// })

		// Set up logic to send the req to the proper provider.
		// with the id and message.

 }
}
